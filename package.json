{
  "name": "transformdata-ts",
  "description": "Нормализация данных",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tsc --watch",
    "build": "tsc",
    "start": "tsc main.ts -t ES2017"
  },
  "dependencies": {
    "typescript": "^4.7.4"
  },
  "devDependencies": {
    "jest": "^28.1.3"
  }
}
